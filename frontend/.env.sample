# Copy this file to .env and fill in values as needed.

# Port the frontend dev server listens on (UDP) for messages from
# agent_red, printed to the terminal running Vite. Separate from
# AGENT_COMM_PORT (used between agent_red and agent_blue) so the two
# listeners don't race to bind the same UDP port.
FRONTEND_COMM_PORT=8766
